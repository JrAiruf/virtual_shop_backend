datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

model HomeImages {
    id       Int    @id @default(autoincrement())
    url      String @unique
    position Int
    xAxis    Int
    yAxis    Int
}
